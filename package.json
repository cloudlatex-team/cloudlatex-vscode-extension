{
  "name": "cloudlatex",
  "displayName": "Cloud LaTeX",
  "description": "",
  "publisher": "moritanian",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.44.0"
  },
  "categories": [
    "Other"
  ],
  "license": "MIT",
  "activationEvents": [
    "workspaceContains:.cloudlatex.json",
    "onView:cloudlatex-commands"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "views": {
      "cloudlatex": [
        {
          "id": "cloudlatex-commands",
          "name": "Commands",
          "when": "config.cloudlatex.enabled"
        }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "cloudlatex",
          "title": "cloudlatex",
          "icon": "resources/icon.png"
        }
      ]
    },
    "configuration": {
      "type": "object",
      "title": "Cloudlatex",
      "properties": {
        "cloudlatex.enabled": {
          "type": "boolean",
          "default": false,
          "description": "set true if enable cloudlatex plugin"
        },
        "cloudlatex.outDir": {
          "type": "string",
          "default": ".workspace",
          "description": "directory to output compile result"
        },
        "cloudlatex.backend": {
          "type": "string",
          "enum": [
            "cloudlatex"
          ],
          "default": "cloudlatex",
          "description": "backend web application"
        },
        "cloudlatex.autoBuild": {
          "type": "boolean",
          "default": true,
          "description": "set true if automatically build when any files are saved."
        },
        "cloudlatex.endpoint": {
          "type": "string",
          "default": "https://cloudlatex.io/api",
          "description": "API endpoint"
        },
        "cloudlatex.email": {
          "type": "string",
          "default": 0,
          "description": "E-mail address"
        },
        "cloudlatex.client": {
          "type": "string",
          "default": "",
          "description": "client id"
        },
        "cloudlatex.token": {
          "type": "string",
          "default": "",
          "description": "access token"
        },
        "cloudlatex.projectId": {
          "type": "number",
          "default": 0,
          "description": "projectId. *Do not mistake this value, otherwise your files might be overwritten."
        }
      }
    },
    "commands": [
      {
        "command": "cloudlatex.refreshEntry",
        "title": "Refresh",
        "category": "Cloud LaTeX",
        "icon": {
          "light": "resources/light/refresh.svg",
          "dark": "resources/dark/refresh.svg"
        }
      },
      {
        "command": "cloudlatex.compile",
        "title": "Compile",
        "category": "Cloud LaTeX"
      },
      {
        "command": "cloudlatex.reload",
        "title": "Reload",
        "category": "Cloud LaTeX"
      },
      {
        "command": "cloudlatex.open",
        "title": "Open",
        "category": "Cloud LaTeX"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "cloudlatex.refreshEntry",
          "when": "view == cloudlatex-commands",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "yarn run compile",
    "compile": "tsc -p ./",
    "lint": "eslint src --ext ts",
    "watch": "tsc -watch -p ./",
    "pretest": "yarn run compile && yarn run lint",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/glob": "^7.1.1",
    "@types/mocha": "^7.0.2",
    "@types/node": "^13.11.0",
    "@types/node-fetch": "^2.5.5",
    "@types/vscode": "^1.44.0",
    "@typescript-eslint/eslint-plugin": "^2.26.0",
    "@typescript-eslint/parser": "^2.26.0",
    "eslint": "^6.8.0",
    "glob": "^7.1.6",
    "mocha": "^7.1.1",
    "typescript": "^3.8.3",
    "vscode-test": "^1.3.0"
  },
  "dependencies": {}
}
