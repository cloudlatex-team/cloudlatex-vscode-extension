export type VSConfig = {
  enabled: boolean,
  outDir: string,
  backend: string,
  autoBuild: boolean,
  endpoint: string,
  projectId: number
};

export type Account = {
  token: string,
  email: string,
  client: string
};

